# CoreVision v2 Dependencies
# Install with: pip install -r requirements.txt

# ── Deep Learning ──────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
timm>=0.9.0                # EfficientNetV2-S backbone
open-clip-torch>=2.20.0    # CLIP zero-shot brand classification

# ── Detection ─────────────────────────────────
ultralytics>=8.0.0         # YOLOv8

# ── OCR ───────────────────────────────────────
paddlepaddle>=2.6.0        # PaddleOCR backend
paddleocr>=2.7.0           # Multi-language OCR

# ── Video & Image Processing ──────────────────
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# ── Backend ───────────────────────────────────
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6    # Required for file uploads

# ── Optional: EasyOCR fallback ────────────────
# easyocr>=1.7.0           # Uncomment if PaddleOCR unavailable
